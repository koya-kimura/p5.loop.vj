class layer06 extends baseLayer {
    constructor() {
        super();
        this.layerIndex = 6;
    }

    draw1(tex, alpha) {
        gif(IMAGES["sea"], lvm.zigzagEase(4), tex, alpha);
    }

    draw2(tex, alpha) {
    }

    draw3(tex, alpha) {
    }

    draw4(tex, alpha) {
    }

    draw5(tex, alpha) {
    }

    draw6(tex, alpha) {
    }

    draw7(tex, alpha) {
    }

    draw8(tex, alpha) {
    }
}